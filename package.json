{
  "name": "runenv-python-3",
  "scripts": {
    "cleanup": "pnpm -r cleanup",
    "build": "turbo run build",
    "build-descriptors": "turbo run build-descriptors",
    "postbuild-publish": "turbo run postbuild-publish",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "catalog:",
    "@changesets/cli": "catalog:",
    "tar": "catalog:",
    "unzipper": "catalog:"
  }
}
