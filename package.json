{
  "name": "runenv-python-3",
  "scripts": {
    "cleanup": "pnpm -r cleanup",
    "reset": "pnpm -r reset && rm -rf ./node_modules ./.turbo",
    "build": "turbo run build",
    "after-prebuild": "turbo run after-prebuild",
    "before-publish": "pnpm -r before-publish",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "update-sdk": "pl-blocks-deps-updater"
  },
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "catalog:",
    "@changesets/cli": "catalog:",
    "@platforma-sdk/blocks-deps-updater": "catalog:"
  }
}
